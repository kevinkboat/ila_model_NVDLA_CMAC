// File: cmac_helpers.cc

#include <ilang/ilang++.h>
#include <cmac_helpers.h>

namespace ilang {

    // int BvToInt(const ExprRef& bv){
    //     auto len = bv.bit_width()

    //     for(auto i = 0; i < len; i++){
    //         if (SelectBit(bv, i) == Bv)
    //     }

    // }



    ExprRef IntToBv(int num, int len_bv)
  
} // namespace ilang 